{
  "name": "ci-cd-starter",
  "version": "1.0.0",
  "private": true,
  "description": "Starter Node.js app for a CI/CD workshop (lint + test + container + deploy hook + smoke test).",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "lint": "eslint .",
    "test": "jest --runInBand",
    "build": "echo \"No build step for this demo\""
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "eslint": "^9.7.0",
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}